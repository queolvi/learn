#include <iostream>
namespace base_{
auto setup_prog(const int& frst, const int& scnd);

};
int main () {

  bool wt_choice;
  int num_1 = 0, num_2 = 0;
  
  std::cout << "Enter first num! \n";
  std::cin >> num_1;
  
  std::cout << "Enter first num! \n";
  std::cin >> num_1;
  
  return 0;
}
auto base_::setup_prog(const int& frst, const int& scnd, const bool& choice, ) {
  
}
